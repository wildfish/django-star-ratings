language: python

sudo: false

python:
  - "2.7"
  - "3.4"

install:
  - pip install codecov
  - pip install -r tests/requirements.txt

script:
  - coverage run --source='star_ratings' runtests.py

after_success:
  - codecov